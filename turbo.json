{
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "generate:components": {
      "outputs": ["src/**"]
    },
    "bundlesize": {
      "dependsOn": ["generate:components", "build"]
    }
  }
}
