{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "generate:icons": {
      "outputs": ["src/**"]
    },
    "bundlesize": {
      "dependsOn": ["generate:icons", "build"]
    }
  }
}
