{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "packages:clean": "lerna exec -- rm -rf node_modules",
    "generate:icons": "lerna run generate:icons",
    "generate:react-simple-icons": "lerna exec yarn generate:icons react-simple-icons",
    "test": "echo \"Not Test\" && exit 0"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
