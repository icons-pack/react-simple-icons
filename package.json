{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "packages:clean": "lerna exec -- rm -rf node_modules",
    "generate:components": "lerna run generate:components",
    "generate:react-simple-icons": "lerna exec yarn generate:components react-simple-icons",
    "test": "echo \"Not Test\" && exit 0"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
