{
  "name": "root",
  "private": false,
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "packages:clean": "lerna exec -- rm -rf node_modules",
    "release": "lerna publish --exact --skip-npm --conventional-commits --yes",
    "compile:icons": "lerna run compile",
    "compile:react-simple-icons": "lerna exec yarn compile react-simple-icons"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
